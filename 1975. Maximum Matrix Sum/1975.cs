public class Solution {
    public long MaxMatrixSum(int[][] matrix) {
        bool isZero = false;
        int negativeCount = 0;
        int absolute = 0;
        int minValue = 100000;

        foreach (var i in matrix)
        {
            foreach (var j in i)
            {
                
            }
        }
    }
}